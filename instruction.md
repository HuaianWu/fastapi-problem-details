我们给外部团队提供的接口，参数校验错误和业务异常现在长得不一样，客户端只能为 FastAPI 单独写两套解析。希望能有一个可选的 Problem Details 响应模式：开启后，HTTP 异常和请求校验错误按同一套标准字段返回，校验错误仍要保留字段定位；未开启时保持现在的响应格式。应用自己注册的异常处理器不能被悄悄覆盖，OpenAPI 文档也要能反映实际会返回的错误结构，WebSocket 校验和已有响应状态不要受到影响。
